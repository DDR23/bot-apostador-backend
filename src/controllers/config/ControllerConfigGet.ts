// import { Socket } from "socket.io";
// import { TypeConfigTenis } from "../../types/TypeConfig";

// export default function ControllerConfigGet(socket: Socket, user: TypeConfigTenis) {
//   const response = `dados do usuários ${user} retornando para o front`
//   socket.emit('config_get_res', response )
//   console.log(socket.id);
//   console.log(user);
// }
